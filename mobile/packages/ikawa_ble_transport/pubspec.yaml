name: ikawa_ble_transport
description: BLE abstraction and adapters for IKAWA app.
version: 0.1.0
publish_to: "none"
resolution: workspace

environment:
  sdk: "^3.11.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
