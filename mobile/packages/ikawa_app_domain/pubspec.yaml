name: ikawa_app_domain
description: Domain services for IKAWA app orchestration.
version: 0.1.0
publish_to: "none"
resolution: workspace

environment:
  sdk: "^3.11.5"

dependencies:
  ikawa_ble_transport:
    path: ../ikawa_ble_transport
  ikawa_protocol_core:
    path: ../ikawa_protocol_core

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
